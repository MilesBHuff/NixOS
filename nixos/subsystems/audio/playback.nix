#!/usr/bin/env nix eval -f
{config, pkgs, lib, var, ...}: {
    #TODO: Install Quod Libet, Ex Falso, MKV, VLC, EasyEffects
    #TODO: Install something that does ReplayGain tagging per EBU R128 (like `r128gain`)
}
